// 2014-10-09 mise à jour date
function CHE_baseProduction()
{
	// affiche le formulaire de recherche de la base de production Sudoc
	application.activeWindow.command("che", false);
}
function CHE_basesExternes()
{
// affiche le formulaire de recherche des bases externes
	application.activeWindow.command("DIS CHE", false);
}
